import { generateFleetProposal } from '@/lib/proposalGenerator';

// Generate the proposal PDF immediately
generateFleetProposal();

console.log('Fleet Management System Proposal PDF generated successfully!');
